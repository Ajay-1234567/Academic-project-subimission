{
  "name": "academic-project-submission-portal",
  "version": "1.0.0",
  "engines": {
    "node": "22.12.0"
  },
  "description": "Unified Academic Project Submission Portal",
  "scripts": {
    "start": "node backend/index.js",
    "build": "npm run install-all && cd frontend && npm run build -- --output-path=../public && cd .. && mv public/browser/* public/ && rm -rf public/browser",
    "vercel-build": "npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm start --prefix backend\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql2": "^3.20.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
