{
  "name": "academic-portal-final",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mysql2": "^3.17.2"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && (cp -rv frontend/dist/frontend/browser/. . || cp -rv frontend/dist/frontend/. . || true)"
  }
}
